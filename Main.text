<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Roplet Games</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background-color: #ffffff;
            color: #000000;
            font-family: 'Arial', sans-serif;
            overflow-x: hidden;
            height: 100vh;
            display: flex;
            flex-direction: column;
        }

        header {
            padding: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .logo {
            display: flex;
            flex-direction: column;
            align-items: center;
            text-transform: uppercase;
            font-weight: bold;
            font-size: 2.5rem;
            letter-spacing: 5px;
            color: white;
        }

        .logo-line1 {
            font-size: 3rem;
        }

        .logo-line2 {
            font-size: 1.8rem;
            margin-top: -10px;
        }

        /* Contenu principal */
        main {
            flex: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 20px;
            text-align: center;
        }

        /* Section présentation */
        .presentation {
            margin-bottom: 80px;
            max-width: 800px;
        }

        .title {
            font-size: 4rem;
            margin-bottom: 30px;
            text-transform: uppercase;
            letter-spacing: 3px;
            animation: rgbAnimation 5s linear infinite;
        }

        .presentation-text {
            font-size: 1.5rem;
            line-height: 1.6;
        }

        /* Section Coming Soon */
        .coming-soon {
            margin-top: 50px;
        }

        .coming-soon-title {
            font-size: 5rem;
            text-transform: uppercase;
            letter-spacing: 5px;
            animation: rgbAnimation 5s linear infinite, float 3s ease-in-out infinite;
        }

        /* Animations */
        @keyframes rgbAnimation {
            0% { color: rgb(255, 0, 0); }
            16% { color: rgb(255, 0, 255); }
            32% { color: rgb(0, 0, 255); }
            48% { color: rgb(0, 255, 255); }
            64% { color: rgb(0, 255, 0); }
            80% { color: rgb(255, 255, 0); }
            100% { color: rgb(255, 0, 0); }
        }

        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }

        /* Effets de particules */
        .particles {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            pointer-events: none;
        }

        .particle {
            position: absolute;
            background-color: rgba(255, 255, 255, 0.5);
            border-radius: 50%;
            animation: floatParticle linear infinite;
        }

        @keyframes floatParticle {
            0% {
                transform: translateY(0) translateX(0);
                opacity: 0;
            }
            10% {
                opacity: 1;
            }
            90% {
                opacity: 1;
            }
            100% {
                transform: translateY(-100vh) translateX(100px);
                opacity: 0;
            }
        }

        /* Responsive */
        @media (max-width: 768px) {
            .logo-line1 {
                font-size: 2rem;
            }
            
            .logo-line2 {
                font-size: 1.2rem;
            }
            
            .title {
                font-size: 2.5rem;
            }
            
            .presentation-text {
                font-size: 1.2rem;
            }
            
            .coming-soon-title {
                font-size: 3rem;
            }
        }
    </style>
</head>
<body>
    <!-- Header avec logo -->
    <header>
        <div class="logo">
            <div class="logo-line1">GODPLEY</div>
            <div class="logo-line2">BEAUTIFUL</div>
        </div>
    </header>

    <!-- Contenu principal -->
    <main>
        <!-- Section présentation -->
        <section class="presentation">
            <h1 class="title">Présentation</h1>
            <p class="presentation-text">
                Bonjour je me présente je m'appelle Badis j'ai 16 ans et je suis développeur de jeu indépendant.
            </p>
        </section>

        <!-- Section Coming Soon -->
        <section class="coming-soon">
            <h2 class="coming-soon-title">Coming Soon</h2>
        </section>
    </main>

    <!-- Effets de particules -->
    <div class="particles" id="particles"></div>

    <script>
        // Création des particules
        const particlesContainer = document.getElementById('particles');
        const particleCount = 50;
        
        for (let i = 0; i < particleCount; i++) {
            const particle = document.createElement('div');
            particle.classList.add('particle');
            
            // Taille aléatoire entre 1px et 3px
            const size = Math.random() * 2 + 1;
            particle.style.width = `${size}px`;
            particle.style.height = `${size}px`;
            
            // Position aléatoire
            particle.style.left = `${Math.random() * 100}vw`;
            particle.style.top = `${Math.random() * 100}vh`;
            
            // Durée d'animation aléatoire entre 10s et 20s
            const duration = Math.random() * 10 + 10;
            particle.style.animationDuration = `${duration}s`;
            
            // Délai aléatoire
            particle.style.animationDelay = `${Math.random() * 10}s`;
            
            particlesContainer.appendChild(particle);
        }
    </script>
</body>
</html>